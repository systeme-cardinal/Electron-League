const {ipcRenderer} = require('electron');

function matchmaking() {
    var document = document.g
}

//ipcRenderer.send("islogin");



ipcRenderer.on('readycheck', function(event, result) {
    
});